<Window x:Class="MouseTranslator.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MouseTranslator.App"
        mc:Ignorable="d"
        Title="Mouse Select Translator"
        Height="260"
        Width="440"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock FontSize="22"
                   FontWeight="SemiBold"
                   Text="Mouse Select Translator" />
        <TextBlock Grid.Row="1"
                   Margin="0,12,0,0"
                   TextWrapping="Wrap"
                   Text="The app is running in the tray. Drag-select text in Notepad or a browser, then release the mouse to trigger translation." />
        <TextBlock Grid.Row="2"
                   Margin="0,12,0,0"
                   TextWrapping="Wrap"
                   Text="Hotkeys: Ctrl+Shift+T toggles enable state, Esc hides the overlay." />
        <TextBlock Grid.Row="3"
                   Margin="0,16,0,0"
                   VerticalAlignment="Top"
                   Opacity="0.7"
                   Text="{Binding Details}" />
    </Grid>
</Window>
